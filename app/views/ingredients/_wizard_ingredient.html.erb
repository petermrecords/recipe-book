<li class='wizard-list-item font-cursive'>
	<span class='wizard-list-display'><%= ingredient.display_ingredient_list %></span>
	<span class='float-right'>
		<%= link_to icon('fas', 'pen'), edit_recipe_ingredient_path(@recipe, ingredient), remote: true %>
		<%= link_to icon('fas', 'times'), recipe_ingredient_path(@recipe, ingredient), method: :delete, remote: true, data: { confirm: 'Are you sure you want to delete this?' } %>
	</span>
</li>